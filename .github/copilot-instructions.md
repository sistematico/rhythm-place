# Copilot Instructions

Use Bun for all package management and project commands in this repository.

- Prefer `bun install`, `bun add`, `bun remove`, `bunx`, and `bun run <script>`.
- Do not use or recommend `pnpm`, `npm`, or `yarn`.
- Keep generated examples, docs, and maintenance commands Bun-first.